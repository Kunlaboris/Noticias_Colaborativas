import React from 'react';
import { TopBar } from '../components/TopBar';
import { Menu } from '../components/Menu';

export const Home = () => {
  return (
    <header>
      <TopBar />
      <Menu />
    </header>
  );
};
